# TrendGuesser Production Environment Variables
# These variables should be configured in Vercel Project Settings

# Neon PostgreSQL Database (REQUIRED)
# NEON_DATABASE_URL=postgres://user:password@hostname/database

# Security
# API key for database initialization (used by Vercel cron job)
# Generate a strong random key for production use
# DB_INIT_API_KEY=your_production_api_key

# Pexels API for images
# Get a free API key from https://www.pexels.com/api/
# PEXELS_API_KEY=your_pexels_api_key

# API Configuration
# Increase the timeout for API routes if needed (in milliseconds)
# API_TIMEOUT=30000

# Application Settings
NEXT_PUBLIC_APP_NAME=TrendGuesser
NEXT_PUBLIC_APP_DESCRIPTION="A game where you guess the trend of search terms over time"
NEXT_PUBLIC_APP_URL=https://your-production-domain.com

# Production Mode
NODE_ENV=production